import React from "react"

export default function Navbar(){
    return(
        <nav className="fixed top-0 left-0 w-full p-4 bg-blue-500 text-white transition duration-500">
            <p>Acceuil</p>
        </nav>
    )
}